@extends('layouts.app') @section('title','Portfolio') @section('content')<x-page-header title="Portfolio"/><div class="row g-3"><div class="col-md-5"><form method="POST" action="{{ route('portfolio.category') }}" class="content-card">@csrf<h5>New category</h5><input name="name" class="form-control" required><button class="btn btn-primary mt-2">Create</button></form></div><div class="col-md-7"><form method="POST" action="{{ route('portfolio.attach') }}" class="content-card">@csrf<h5>Publish gallery photo</h5><select name="category_id" class="form-select">@foreach($categories as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select><select name="photo_id" class="form-select">@foreach($photos as $p)<option value="{{ $p->id }}">{{ $p->filename }}</option>@endforeach</select><button class="btn btn-primary">Publish</button></form></div></div>@foreach($categories as $c)<div class="content-card mt-3"><h4>{{ $c->name }}</h4><p>{{ $c->photos->count() }} published photographs</p></div>@endforeach @endsection
