@extends('layouts.app') @section('title',($member->exists?'Edit':'Add').' staff — PhotoHub') @section('content')<x-page-header :title="$member->exists?'Edit staff member':'Add staff member'"/><form class="content-card auto-height" method="POST" action="{{ $member->exists?route('staff.update',$member):route('staff.store') }}">@csrf @if($member->exists)@method('PUT')@endif<div class="row g-3"><div class="col-md-6"><label class="form-label">Name</label><input name="name" required class="form-control" value="{{ old('name',$member->user?->name) }}"></div>@unless($member->exists)<div class="col-md-6"><label class="form-label">Email</label><input name="email" type="email" required class="form-control" value="{{ old('email') }}"></div>@endunless<div class="col-md-6"><label class="form-label">Phone</label><input name="phone" class="form-control" value="{{ old('phone',$member->user?->phone) }}"></div><div class="col-md-6"><label class="form-label">Job title</label><input name="job_title" class="form-control" value="{{ old('job_title',$member->job_title) }}"></div>@unless($member->exists)<div class="col-md-6"><label class="form-label">Password</label><input name="password" type="password" required class="form-control"></div><div class="col-md-6"><label class="form-label">Confirm password</label><input name="password_confirmation" type="password" required class="form-control"></div>@else<div class="col-md-6"><label class="form-label">Status</label><select name="status" class="form-select">@foreach(['active','inactive'] as $s)<option @selected(old('status',$member->status)===$s) value="{{ $s }}">{{ ucfirst($s) }}</option>@endforeach</select></div>@endunless<div class="col-12"><label class="form-label d-block">Roles</label>@foreach($roles as $role)<label class="form-check form-check-inline"><input class="form-check-input" type="checkbox" name="roles[]" value="{{ $role->id }}" @checked(in_array($role->id,old('roles',$member->exists?$member->roles->pluck('id')->all():[])))> {{ $role->name }}</label>@endforeach</div></div><button class="btn btn-primary mt-4">Save staff member</button></form>@endsection
