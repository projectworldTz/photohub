@extends('layouts.app') @section('title','Staff — PhotoHub') @section('content')<x-page-header title="Staff" subtitle="Manage studio access and responsibilities."><a href="{{ route('staff.create') }}" class="btn btn-primary">Add staff member</a></x-page-header><div class="content-card auto-height"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Employee</th><th>Number</th><th>Roles</th><th>Status</th><th></th></tr></thead><tbody>@foreach($members as $m)<tr><td><strong>{{ $m->user->name }}</strong><small class="d-block text-muted">{{ $m->user->email }} · {{ $m->job_title }}</small></td><td>{{ $m->employee_number }}</td><td>@foreach($m->roles as $role)<span class="badge text-bg-light">{{ $role->name }}</span>@endforeach</td><td><span class="badge text-bg-{{ $m->status==='active'?'success':'secondary' }}">{{ ucfirst($m->status) }}</span></td><td class="text-end"><a class="btn btn-sm btn-light" href="{{ route('staff.edit',$m) }}">Edit</a></td></tr>@endforeach</tbody></table></div>{{ $members->links() }}</div>@endsection
