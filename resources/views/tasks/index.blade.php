@extends('layouts.app') @section('title','Tasks') @section('content')<x-page-header title="Tasks"/><form method="POST" action="{{ route('tasks.store') }}" class="content-card mb-3">@csrf<div class="row g-2"><div class="col-md-5"><input name="title" class="form-control" placeholder="Task" required></div><div class="col-md-3"><input name="due_at" type="datetime-local" class="form-control"></div><div class="col-md-2"><select name="priority" class="form-select"><option>normal</option><option>high</option><option>urgent</option></select></div><div class="col-md-2"><button class="btn btn-primary">Add task</button></div></div></form><div class="content-card"><table class="table">@foreach($tasks as $t)<tr><td>{{ $t->title }}</td><td>{{ $t->due_at?->format('M j H:i') }}</td><td>{{ ucfirst($t->priority) }}</td><td><form method="POST" action="{{ route('tasks.update',$t) }}">@csrf @method('PUT')<select name="status" onchange="this.form.submit()">@foreach(['todo','in_progress','completed'] as $s)<option value="{{ $s }}" @selected($t->status===$s)>{{ str($s)->replace('_',' ')->title() }}</option>@endforeach</select></form></td></tr>@endforeach</table>{{ $tasks->links() }}</div>@endsection
