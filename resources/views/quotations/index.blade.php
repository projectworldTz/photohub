@extends('layouts.app') @section('title','Quotations') @section('content')<x-page-header title="Quotations"><a href="{{ route('quotations.create') }}" class="btn btn-primary">New quotation</a></x-page-header><div class="content-card"><table class="table">@foreach($quotations as $q)<tr><td><a href="{{ route('quotations.show',$q) }}">{{ $q->quotation_number }}</a></td><td>{{ $q->customer->full_name }}</td><td>{{ number_format((float)$q->total) }}</td><td>{{ ucfirst($q->status) }}</td></tr>@endforeach</table>{{ $quotations->links() }}</div>@endsection
