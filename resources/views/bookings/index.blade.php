@extends('layouts.app') @section('title','Bookings — PhotoHub') @section('content')<x-page-header title="Bookings" subtitle="Schedule and track assignments."><div><a href="{{ route('calendar') }}" class="btn btn-light">Calendar</a> <a href="{{ route('bookings.create') }}" class="btn btn-primary">New booking</a></div></x-page-header><div class="content-card"><div class="table-responsive"><table class="table"><thead><tr><th>Booking</th><th>Customer</th><th>Schedule</th><th>Status</th><th></th></tr></thead><tbody>@forelse($bookings as $b)<tr><td>{{ $b->booking_number }}<small class="d-block">{{ $b->event_type }}</small></td><td>{{ $b->customer->full_name }}</td><td>{{ $b->event_date->format('M j, Y') }} {{ $b->start_time }}</td><td>{{ str($b->status)->replace('_',' ')->title() }}</td><td><a href="{{ route('bookings.show',$b) }}">View</a></td></tr>@empty<tr><td colspan="5">No bookings found.</td></tr>@endforelse</tbody></table></div>{{ $bookings->links() }}</div>@endsection
