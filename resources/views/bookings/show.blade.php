@extends('layouts.app') @section('title',$booking->booking_number) @section('content')<x-page-header :title="$booking->booking_number" :subtitle="$booking->event_type.' for '.$booking->customer->full_name"><a href="{{ route('bookings.edit',$booking) }}" class="btn btn-light">Edit</a> @unless($booking->shoot)<form class="d-inline" method="POST" action="{{ route('bookings.shoot',$booking) }}">@csrf<button class="btn btn-primary">Create shoot</button></form>@endunless</x-page-header><div class="content-card"><h5>{{ $booking->event_date->format('l, F j, Y') }}, {{ $booking->start_time }}–{{ $booking->end_time }}</h5><p>{{ $booking->location }}</p><p>Team: {{ $booking->staff->pluck('user.name')->join(', ') }}</p><hr><h4>Balance: {{ $currentBusiness->currency }} {{ number_format((float)$booking->balance) }}</h4><p>Status: {{ str($booking->status)->replace('_',' ')->title() }}</p></div>@endsection
