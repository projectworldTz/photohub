@extends('layouts.app') @section('title','Leads — PhotoHub') @section('content')<x-page-header title="Leads" subtitle="Track inquiries through conversion."><a href="{{ route('leads.create') }}" class="btn btn-primary">New lead</a></x-page-header><div class="content-card auto-height"><form class="row g-2 mb-3"><div class="col-md-7"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Search leads"></div><div class="col-md-3"><select name="status" class="form-select"><option value="">All statuses</option>@foreach(['new','contacted','interested','quotation_sent','negotiating','won','lost'] as $s)<option @selected(request('status')===$s) value="{{ $s }}">{{ str($s)->replace('_',' ')->title() }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-outline-secondary w-100">Filter</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Lead</th><th>Event</th><th>Budget</th><th>Status</th><th></th></tr></thead><tbody>@forelse($leads as $lead)<tr><td><strong>{{ $lead->name }}</strong><small class="d-block text-muted">{{ $lead->phone }}</small></td><td>{{ $lead->event_type?:'—' }}<small class="d-block text-muted">{{ $lead->event_date?->format('M j, Y') }}</small></td><td>{{ $lead->estimated_budget?number_format((float)$lead->estimated_budget):'—' }}</td><td><span class="badge text-bg-light">{{ str($lead->status)->replace('_',' ')->title() }}</span></td><td class="text-end"><a class="btn btn-sm btn-light" href="{{ route('leads.edit',$lead) }}">Manage</a></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No leads found.</td></tr>@endforelse</tbody></table></div>{{ $leads->links() }}</div>@endsection
