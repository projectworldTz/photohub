@extends('layouts.app') @section('title','Packages — PhotoHub') @section('content')<x-page-header title="Photography packages" subtitle="Configure what your studio sells."><a href="{{ route('packages.create') }}" class="btn btn-primary">New package</a></x-page-header><div class="row g-3">@forelse($packages as $p)<div class="col-md-6 col-xl-4"><div class="content-card auto-height h-100"><div class="d-flex justify-content-between"><span class="badge text-bg-light">{{ $p->category }}</span><span class="badge text-bg-{{ $p->is_active?'success':'secondary' }}">{{ $p->is_active?'Active':'Inactive' }}</span></div><h4 class="mt-3">{{ $p->name }}</h4><h3>{{ $currentBusiness->currency }} {{ number_format((float)$p->price) }}</h3><p class="text-muted">{{ Str::limit($p->description,100) }}</p><div class="small mb-3">{{ $p->edited_photos_count??'Flexible' }} edited photos · {{ $p->delivery_days }} day delivery</div><a href="{{ route('packages.edit',$p) }}" class="btn btn-outline-primary btn-sm">Edit package</a></div></div>@empty<div class="col-12"><div class="content-card text-center text-muted py-5">No packages created.</div></div>@endforelse</div><div class="mt-3">{{ $packages->links() }}</div>@endsection
