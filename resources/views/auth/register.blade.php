@extends('layouts.app')
@section('title', 'Create your studio — PhotoHub')
@section('content')<div class="auth-page py-5"><div class="auth-card register-card"><a href="{{ route('home') }}" class="brand dark text-decoration-none"><i class="bi bi-aperture"></i> PhotoHub</a><h1>Create your studio</h1><p class="text-muted">Start your 14-day workspace trial.</p><form method="POST" action="{{ route('register') }}">@csrf<div class="row g-3">@foreach([['business_name','Business name','text'],['owner_name','Owner name','text'],['email','Email','email'],['phone','Phone','text'],['city','City','text'],['country','Country','text'],['category','Photography category','text'],['currency','Currency (e.g. TZS)','text']] as [$name,$label,$type])<div class="col-md-6"><label class="form-label">{{ $label }}</label><input name="{{ $name }}" type="{{ $type }}" value="{{ old($name, $name === 'country' ? 'Tanzania' : ($name === 'currency' ? 'TZS' : '')) }}" class="form-control @error($name) is-invalid @enderror" required>@error($name)<div class="invalid-feedback">{{ $message }}</div>@enderror</div>@endforeach<div class="col-12"><label class="form-label">Address</label><input name="address" value="{{ old('address') }}" class="form-control"></div><div class="col-md-6"><label class="form-label">Timezone</label><input name="timezone" value="{{ old('timezone','Africa/Dar_es_Salaam') }}" class="form-control" required></div><div class="col-12"><label class="form-label">About your business</label><textarea name="description" class="form-control" rows="3">{{ old('description') }}</textarea></div><div class="col-md-6"><label class="form-label">Password</label><input name="password" type="password" class="form-control @error('password') is-invalid @enderror" required>@error('password')<div class="invalid-feedback">{{ $message }}</div>@enderror</div><div class="col-md-6"><label class="form-label">Confirm password</label><input name="password_confirmation" type="password" class="form-control" required></div></div><button class="btn btn-primary w-100 py-2 mt-4">Create studio</button></form><p class="text-center mt-4 mb-0">Already registered? <a href="{{ route('login') }}">Sign in</a></p></div></div>@endsection
