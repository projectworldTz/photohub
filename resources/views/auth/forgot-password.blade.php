@extends('layouts.app') @section('title','Reset password') @section('content')<div class="auth-page"><form method="POST" action="{{ route('password.email') }}" class="auth-card">@csrf<h1>Reset password</h1><p class="text-muted">We’ll email you a secure reset link.</p>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<input name="email" type="email" class="form-control" required><button class="btn btn-primary w-100 mt-3">Send reset link</button></form></div>@endsection
