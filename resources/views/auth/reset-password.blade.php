@extends('layouts.app') @section('title','Choose password') @section('content')<div class="auth-page"><form method="POST" action="{{ route('password.update') }}" class="auth-card">@csrf<h1>Choose a new password</h1><input type="hidden" name="token" value="{{ $token }}"><input name="email" type="email" value="{{ $email }}" class="form-control mb-2" required><input name="password" type="password" class="form-control mb-2" placeholder="New password" required><input name="password_confirmation" type="password" class="form-control" placeholder="Confirm password" required><button class="btn btn-primary w-100 mt-3">Update password</button></form></div>@endsection
