@extends('layouts.app') @section('title','Customers — PhotoHub') @section('content')
<x-page-header title="Customers" subtitle="Your complete customer directory."><a class="btn btn-primary" href="{{ route('customers.create') }}"><i class="bi bi-plus-lg"></i> New customer</a></x-page-header>
<div class="content-card auto-height"><form class="row g-2 mb-3"><div class="col-md-6"><input class="form-control" name="search" value="{{ request('search') }}" placeholder="Search name, number or phone"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">All statuses</option>@foreach(['active','inactive','archived'] as $s)<option @selected(request('status')===$s) value="{{ $s }}">{{ ucfirst($s) }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-outline-secondary w-100">Filter</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Customer</th><th>Number</th><th>Contact</th><th>Status</th><th></th></tr></thead><tbody>@forelse($customers as $c)<tr><td><strong>{{ $c->full_name }}</strong><small class="d-block text-muted">{{ $c->city }}</small></td><td>{{ $c->customer_number }}</td><td>{{ $c->phone }}<small class="d-block text-muted">{{ $c->email }}</small></td><td><span class="badge text-bg-{{ $c->status==='active'?'success':'secondary' }}">{{ ucfirst($c->status) }}</span></td><td class="text-end"><a href="{{ route('customers.show',$c) }}" class="btn btn-sm btn-light">View</a></td></tr>@empty<tr><td colspan="5" class="text-center text-muted py-5">No customers found.</td></tr>@endforelse</tbody></table></div>{{ $customers->links() }}</div>@endsection
