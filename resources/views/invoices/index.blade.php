@extends('layouts.app') @section('title','Invoices') @section('content')<x-page-header title="Invoices"><a href="{{ route('invoices.create') }}" class="btn btn-primary">New invoice</a></x-page-header><div class="content-card"><table class="table"><thead><tr><th>Invoice</th><th>Customer</th><th>Total</th><th>Balance</th><th>Status</th></tr></thead><tbody>@foreach($invoices as $i)<tr><td><a href="{{ route('invoices.show',$i) }}">{{ $i->invoice_number }}</a></td><td>{{ $i->customer->full_name }}</td><td>{{ number_format((float)$i->total) }}</td><td>{{ number_format((float)$i->balance) }}</td><td>{{ str($i->status)->replace('_',' ')->title() }}</td></tr>@endforeach</tbody></table>{{ $invoices->links() }}</div>@endsection
