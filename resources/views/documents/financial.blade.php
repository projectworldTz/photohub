<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans;color:#172033;font-size:12px}h1{margin-bottom:0}.muted{color:#697386}.right{text-align:right}table{width:100%;border-collapse:collapse;margin-top:30px}th,td{padding:10px;border-bottom:1px solid #ddd}th{background:#f3f5f8}.total{font-size:18px;font-weight:bold}</style></head><body>
<table style="margin:0"><tr><td style="border:0"><h1>{{ $business->name }}</h1><div class="muted">{{ $business->email }} · {{ $business->phone }}</div></td><td class="right" style="border:0"><h2>{{ strtoupper($type) }}</h2><strong>{{ $type==='Invoice'?$document->invoice_number:$document->quotation_number }}</strong></td></tr></table>
<p><strong>Bill to:</strong> {{ $document->customer->full_name }}<br>{{ $document->customer->email }} · {{ $document->customer->phone }}</p>
<table><thead><tr><th>Description</th><th>Quantity</th><th>Unit price</th><th>Total</th></tr></thead><tbody>@foreach($document->items as $item)<tr><td>{{ $item->description }}</td><td>{{ $item->quantity }}</td><td>{{ $business->currency }} {{ number_format((float)$item->unit_price,2) }}</td><td>{{ $business->currency }} {{ number_format((float)$item->total,2) }}</td></tr>@endforeach</tbody></table>
<p class="right">Subtotal: {{ $business->currency }} {{ number_format((float)$document->subtotal,2) }}<br>Discount: {{ number_format((float)$document->discount,2) }}<br>Tax: {{ number_format((float)$document->tax,2) }}</p><p class="right total">Total: {{ $business->currency }} {{ number_format((float)$document->total,2) }}</p>
@if($type==='Invoice')<p class="right">Paid: {{ number_format((float)$document->paid,2) }} · Balance: {{ number_format((float)$document->balance,2) }}<br>Due {{ $document->due_date->format('F j, Y') }}</p>@else<p>Valid until {{ $document->expiry_date->format('F j, Y') }}</p>@endif
<p>{{ $document->notes }}</p><small class="muted">Generated by PhotoHub on {{ now()->format('F j, Y H:i') }}</small></body></html>
