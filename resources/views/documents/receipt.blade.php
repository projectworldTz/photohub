<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans;color:#172033;padding:30px}.box{border:1px solid #ddd;padding:24px}.right{text-align:right}</style></head><body><div class="box"><h1>{{ $business->name }}</h1><div class="right"><h2>RECEIPT</h2><strong>{{ $receipt->receipt_number }}</strong></div><hr>@if($receipt->payment->status==='reversed')<h3 style="color:#b91c1c">REVERSED</h3><p>{{ $receipt->payment->reversal_reason }}</p>@endif<p>Received from <strong>{{ $receipt->customer?->full_name ?: 'Gallery customer' }}</strong></p><h2>{{ $business->currency }} {{ number_format((float)$receipt->amount,2) }}</h2><p>For {{ $receipt->invoice?->invoice_number ?: $receipt->order?->order_number }}</p><p>Method: {{ str($receipt->payment->method)->replace('_',' ')->title() }}<br>Reference: {{ $receipt->payment->transaction_reference ?: '—' }}<br>Date: {{ $receipt->payment->payment_date->format('F j, Y H:i') }}</p><small>Generated by PhotoHub</small></div></body></html>
