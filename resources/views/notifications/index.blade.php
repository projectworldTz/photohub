@extends('layouts.app') @section('title','Notifications') @section('content')<x-page-header title="Notifications"><form method="POST" action="{{ route('notifications.read') }}">@csrf<button class="btn btn-light">Mark all read</button></form></x-page-header><div class="content-card notification-list">@forelse($notifications as $n)<a href="{{ route('notifications.show',$n->id) }}" class="notification-item {{ $n->read_at?'':'unread' }}"><span class="notification-status"></span><span><strong>{{ $n->data['title']??'Notification' }}</strong><small>{{ $n->data['message']??'' }}</small><time>{{ $n->created_at->diffForHumans() }}</time></span></a>@empty<p class="text-muted text-center py-5">You have no notifications.</p>@endforelse{{ $notifications->links() }}</div>@endsection
