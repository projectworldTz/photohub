@extends('layouts.app') @section('title',$gallery->name) @section('content')<div class="auth-page"><form method="POST" action="{{ route('public.gallery.unlock',$gallery->code) }}" class="auth-card text-center">@csrf<h1>{{ $gallery->name }}</h1><p class="text-muted">Enter the private gallery PIN.</p>@if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<input type="password" inputmode="numeric" name="pin" class="form-control text-center" required autofocus><button class="btn btn-primary w-100 mt-3">Open gallery</button></form></div>@endsection
