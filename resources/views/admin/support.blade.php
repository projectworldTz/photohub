@extends('layouts.app') @section('title','Support') @section('content')<x-page-header title="Support requests"/><div class="content-card">@foreach($requests as $r)<div class="border-bottom py-3"><b>{{ $r->subject }}</b><p>{{ $r->message }}</p><form method="POST" action="{{ route('admin.support.update',$r) }}">@csrf @method('PATCH')<select name="status" onchange="this.form.submit()">@foreach(['open','in_progress','resolved','closed'] as $s)<option value="{{ $s }}" @selected($r->status===$s)>{{ $s }}</option>@endforeach</select></form></div>@endforeach{{ $requests->links() }}</div>@endsection
